services:
  approval-bridge:
    build: services/approval-bridge
    ports:
      - "8090:8090"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/health"]
      interval: 30s
      timeout: 5s
      retries: 3
